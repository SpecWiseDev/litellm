# =============================================================================
# LiteLLM Environment Variables for IMQA Gateway
# =============================================================================
# Copy this file and fill in your actual values
# For Railway: Set these in the Railway dashboard, not in a file
# =============================================================================

# -----------------------------------------------------------------------------
# PROVIDER API KEYS
# -----------------------------------------------------------------------------

# Vercel AI Gateway (Primary provider)
# Get from: https://vercel.com/docs/ai-gateway
VERCEL_AI_GATEWAY_API_KEY=

# OpenRouter (Fallback provider)
# Get from: https://openrouter.ai/keys
OPENROUTER_API_KEY=

# Replicate (Open-source models)
# Get from: https://replicate.com/account/api-tokens
REPLICATE_API_KEY=

# -----------------------------------------------------------------------------
# LITELLM CONFIGURATION
# -----------------------------------------------------------------------------

# Master key for API authentication
# Generate a secure key: openssl rand -hex 32
# Prefix with sk- for consistency
LITELLM_MASTER_KEY=sk-your-secure-key-here

# -----------------------------------------------------------------------------
# REDIS (Caching)
# -----------------------------------------------------------------------------
# For Railway: Use ${{Redis.REDISHOST}}, ${{Redis.REDISPORT}}, ${{Redis.REDISPASSWORD}}
# For local: Use localhost:6379

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# -----------------------------------------------------------------------------
# POSTGRESQL (Usage Tracking - Optional)
# -----------------------------------------------------------------------------
# For Railway: Use ${{Postgres.DATABASE_URL}}
# For local: postgresql://user:password@localhost:5432/litellm

DATABASE_URL=

# -----------------------------------------------------------------------------
# OPTIONAL: OpenRouter Settings
# -----------------------------------------------------------------------------

# Your site URL (for OpenRouter tracking)
OR_SITE_URL=https://imqa.ai

# Your app name (shows in OpenRouter dashboard)
OR_APP_NAME=IMQA Gateway

# -----------------------------------------------------------------------------
# OPTIONAL: Observability
# -----------------------------------------------------------------------------

# Langfuse (detailed LLM observability)
# LANGFUSE_PUBLIC_KEY=
# LANGFUSE_SECRET_KEY=
# LANGFUSE_HOST=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# RAILWAY DEPLOYMENT VARIABLES
# -----------------------------------------------------------------------------
# When deploying to Railway, use these variable references:
#
# REDIS_HOST=${{Redis.REDISHOST}}
# REDIS_PORT=${{Redis.REDISPORT}}
# REDIS_PASSWORD=${{Redis.REDISPASSWORD}}
# DATABASE_URL=${{Postgres.DATABASE_URL}}
#
# Railway will automatically inject the correct values
# -----------------------------------------------------------------------------
